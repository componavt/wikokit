
Kleinberg algorithm and Wikipedia Java API

Список ф-ций/целей/свойств (API) для работы с MySQL Wikipedia
!) нужно сделать
+) сделано
-) не будет сделано (с указанием причины)
?) нужно ли делать?

1111111111111111
1 Первая очередь (минимальный набор возможностей - необходимо реализовать для демонстрации возможностей)
1111111111111111
+) Фильтрация с помощью чёрного списка категорий, использовать CategiryBlackList.skip_list
+) При визуализации кластеров: не выводить статьи как узлы, а перечислять внутри кластера текстом (т.е. будут повторяться!)
!) Документация: указать архитектуру Synarcher (=Kleinberg.jar + TGWikiBrowser.jar). 
Т.е. деление на алгоритмы (поиск синонимов, кластеризация) и обращение к БД (Kleinberg.jar) и собственно визуализацию (TGWikiBrowser.jar). 
Т.о. код TGWikiBrowser.jar показывает пример обращения к MySQL Википедия и использования алгоритмов поиска посредством Kleinberg.jar. 

?) Как оценить качество кластеризации: полнота, точность.
!) Вычислить лучший кластер (суммарное x или среднее x?, если учитывать Y?). Пересортировать список синонимов так, что вначале идут слова из этих лучших кластеров (не все, а больше некоторого x).
!) LinkBaseSet.java:CreateBaseSet() -> Links.java:
    public static Article[] GetLToByLFrom(SessionHolder session, int l_from, int n_limit)
    select first n links in article (not first n links in table)

!) ошибка печати graphviz
}W11953 [label="11953\nЭКИП\nx=0.030325988\ny=0.014363836",shape=invtriangle,style=filled,fillcolor=grey];

!) Категории: 
!.1 добавить ф. createArticleWithCategories() (см. Links) в Links.getLFromByLTo()
!.2 добавить тесты по обработке (сохранению данных о) категорий

!) Дать список многозначных слов (метка Disambig id категории 11694.
Примеры многозначных слов: Плазма, Галактика, Газ, Теория_относительности).
Сколько всего в БД явно указанных многозначных слов?
!) Написать интерфейс к Wikipedia (ru & en) наподобие GATE 3.0\plugins\google 

+) Redirect:
   +.1 Не учитывать redirect статью в качестве отдельной статьи, например:
Жданов, Василий Александрович (Перенаправлено с Василий Александрович Жданов)
  
   !.2 (уточнить - сделано?) Особая обработка редирект (redirect) статей. У них нет категорий => не фильтруются по чёрному списку категорий!

!) Не обновляются db_host в .wikibrowser.server.props


2222222222222222
2 Вторая очередь (необходимый набор возможностей, чтобы программа была полезной)
2222222222222222
!.5) Оптимизация: хеш black-list, white-list - сначала категории просматриваются в этих списках, и только затем - обращение к БД. Результаты записываются в эти же списки.
!.6) skip Портал: если русская БД

+3) First-letter is upper
!.1) Словосочетания (без знака подчёркивания в строке ввода) не рисуются, хотя поиск для них выполняется.

!1) Особый способ обработки слов, которые имеют категорию "Disambig" (Плазма) и "Disambiguation". (См. Labour). 
Возможный алгоритм: 
- взять исходящие ссылки;
- каждую обрабатывать как страницу с исходным (искомым) словом.

!2) Под вопросом: пропускать ли недоделанные статьи (Stub).
see: section "Small articles that don't contain Stub or Disambiguate message
at page: http://meta.wikimedia.org/wiki/Requests_for_queries

!3) Визуализация, выбрать:
- www.jgraph.com + http://freshmeat.net/projects/jgraphaddons/
- www.touchgraph.com

!4) Flash synonyms tab header, when synonyms are listed, but the tab was hidden.
Flash Article tab, when Help '?' button was pressed.

!) Два окна (либо два графа в одном окне): граф статей, параллельный (соотв-щий) граф категорий. Выбор, навигация статьи (категории) может (опционально) выбирать, навигировать подсвечивать граф категорий (статей).


Эвристики:
!) В один узел объединялись статьи, имена которых отличаются незначительно (регистром или пунктуацией).
!) Удалили все статьи, начинающиеся со слов: "List of..." (это просто наборы ссылок).
?!) Плотность ссылок = число слов/число ссылок. Гипотеза: если ссылок много, то это возможно список "List of...", т.е. не рассматривать.

?!) Вычислять значения таблицы pagelinks по мере необходимости.
?!) Написать программу наподобие mwdumper для refreshLinks

!) Switch: turn on/off randomize in the Kleinberg algorithm (поправить - сейчас не работает)

!) interwiki сейчас не работает, см. 
http://www.mediawiki.org/wiki/Proposed_Database_Schema_Changes#Interwiki_Links_table
 

3333333333333333
3 Третья очередь (дополнительные возможности)
3333333333333333
!0) "Right click menu: Pin Node" - stay node pinned, don't hide when "Hide All" and "Collapse" are selected
!0.1) "Right click menu: Show categories" - show categories for the node 
!1) re-sort edges: Collections.sort(edges, Edge.WEIGHT_ORDER);  REM пересортировка рёбер, сложность O(N), т.к. нужно обновить порядок только тех рёбер, которые смежны вершине v1
+2) Black-list нарисовать в виде дерева с checkbox'ами, пользователь выбирает - какие категории не учитывать. Это заменит сегодняшний вариант: удаление всех категорий на большом расстоянии (не интеллектуально) от указанных пользователем.
!4) Нарисовать иконку программы: статическую и динамическую
!5) Оптимизация: Links.GetLToByLFromIN replace SQL IN() by =
!6) list vs. category (List of astronomers). Учитывать, что списки (List ...) - это не просто страницы, а подобие категорий. Вариант: если в названии страницы есть List..., то пропускать вовсе.
!7) test http://sourceforge.net/projects/browserlaunch2
!8) добавить параметр в интерфейс пользователя: max_steps - ограничивает число просмотренных категорий, для прерывания поиска, это альтернатива параметру - глубина поиска черного списка категорий. max_steps реализован в CategoryBlackList.getCategoryUpIteratively()


4444444444444444
4 Четвёртая очередь (о коде)
4444444444444444
!1) Node.links_out[], links_in[] - инициализация не null, а пустой массив new int[0].

замечания: windows версия graphviz (dot.exe) может выдавать такие ошибки (в linux всё работает):
Warning: C8101 -> C8124: head not inside head cluster cluster8124
