#summary (./wikt_parser/doc) How to create, edit and load empty Wiktionary parsed database into MySQL.
#labels Phase-Implementation,MySQL,MRD,parsing

It should be done before the parsing of the Wiktionary database.

= Details =

  * Edit `wikt_parsed_empty.mwb` file in MySQL Workbench (see [MySQLWorkbench]), export it to `wikt_parsed_empty.sql`
  * Make substitution in `wikt_parsed_empty.sql` in VIM by RE: 
{{{
%s/`mydb`\.//g
}}}
  * Create DB in MySQL, e.g. `ruwikt20090122_parsed`
  * `USE ruwikt20090122_parsed`
  * `SOURCE wikt_parsed_empty.sql`

= Misc =

== Index ==

CREATE TABLE test (id INT, INDEX(id));

ALTER TABLE `relation` DROP INDEX `idx_software_key` ,
ADD INDEX `idx_software_key` ( `software_key` ( 3 ) );